%li.artist.thumb.entity
  .image.largesquare
    = link_to artist_path(artist), :class => "thumbnail square" do
      = image_tag(artist.image, :alt => artist.name)
  .meta
    %h3= link_to artist.name, artist_path(artist)
    .bio= artist.bio
  .loading

